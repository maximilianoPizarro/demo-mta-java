{{- if .Values.app.enabled }}
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: {{ include "mta-demo.imageStream" . }}
  labels:
    {{- include "mta-demo.labels" . | nindent 4 }}
spec:
  lookupPolicy:
    local: true
{{- end }}
