{{- if and .Values.app.enabled .Values.app.route.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "mta-demo.appName" . }}
  labels:
    {{- include "mta-demo.labels" . | nindent 4 }}
spec:
  to:
    kind: Service
    name: {{ include "mta-demo.appName" . }}
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
{{- end }}
